

task :default => :html

desc "Use pdoc to generate HTML documentation for Perl modules here"
task :html do 
  exe="perlmod2www.pl"
  opts="-target html_mods -source ./mods"
  cmd="#{exe} #{opts}"
  system(cmd)
end
