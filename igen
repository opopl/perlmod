#!/usr/bin/make -f

mkdir:=./mk/iall/

install_modules_mk:=$(mkdir)/install_modules.mk

# Generating script
genpl:=./iall.pl

all: install_modules_mk

install_modules_mk: $(install_modules_mk)

$(install_modules_mk): $(genpl)
	perl $(genpl)

gen_dat remove_dat:
	perl $(genpl) --$@

